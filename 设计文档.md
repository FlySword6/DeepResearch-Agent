# DeepResearch-Agent 设计文档

> 多智能体自动化研究系统
> 当前版本：v2.0
> 最后更新：2026-09-07

---

## 1. 项目定位

DeepResearch-Agent 是一个面向复杂研究任务的多智能体系统。用户输入问题后，系统会先进行轻量级任务路由：简单事实型问题直接走联网搜索并生成答案；复杂调研、趋势分析、行业研究类任务进入多 Agent 工作流，完成任务拆解、资料检索、网页阅读、知识库召回、报告撰写和质量审查。

当前项目在原有 LangGraph 多智能体流程基础上，新增 Harness Agent Runtime 双引擎切换，并将 RAG 知识库从单路向量检索升级为 Milvus 向量检索、BM25 关键词检索、RRF 融合排序和父子节点上下文补全。

---

## 2. 核心能力

| 能力 | 当前实现 | 关键文件 |
| --- | --- | --- |
| 智能任务路由 | 简单问题直接搜索，复杂问题进入多 Agent | `app/services/query_router.py` |
| 双工作流引擎 | LangGraph / Harness Agent Runtime 可切换 | `app/workflow/engine.py`, `app/harness/runtime.py` |
| 多 Agent 协作 | Planner、Researcher、Writer、Reviewer | `app/agents/` |
| Tool Router | 统一管理 search、browse、analyze、rag、read_workspace | `app/tools/router.py` |
| Hybrid RAG | Milvus + BM25 + RRF + 父子节点 | `app/rag/retriever.py` |
| 实时流式展示 | SSE 推送 Agent 状态和报告分块 | `app/workflow/events.py`, `app/web/src/App.jsx` |
| 多任务并行 | 每个任务独立 SSE 队列，支持切换运行中任务 | `app/services/task_manager.py` |
| Workspace 文件 | 每个任务独立上传参考文件，Agent 可读取 | `app/services/workspace.py`, `app/tools/workspace_reader.py` |
| Profile / Skills | 用户偏好和技能注入 Agent Prompt | `app/services/profile_service.py`, `app/services/skill_service.py` |
| 报告导出 | Markdown / PDF 下载 | `app/services/report_service.py`, `app/utils/pdf_utils.py` |

---

## 3. 技术栈

| 层级 | 技术 | 用途 |
| --- | --- | --- |
| 后端语言 | Python 3.11+ | Agent、RAG、API 服务 |
| Web 框架 | FastAPI | REST API、SSE、文件上传 |
| Agent 编排 | LangGraph | 有状态图工作流 |
| Agent Runtime | Harness Agent Runtime | 顺序式 Agent 调度运行时 |
| LLM 调用 | OpenAI / Anthropic / DashScope 兼容接口 | Planner/Researcher/Writer/Reviewer |
| 向量数据库 | Milvus | RAG child chunk 向量检索 |
| 向量兜底 | ChromaDB | Milvus 不可用时本地降级 |
| 关键词检索 | BM25 / rank-bm25 | 精确词、缩写、数字、实体召回 |
| 融合排序 | RRF | 融合向量检索和 BM25 排名 |
| 元数据存储 | SQLite | 任务、报告、技能、用户偏好 |
| 运行依赖 | Redis | 可选任务队列/Worker 运行支持 |
| 前端 | React 18 + Vite + Tailwind CSS | 研究界面、知识库、历史记录 |
| 实时通信 | SSE | Agent 轨迹和报告流式渲染 |
| 部署 | Docker Compose | Redis、Milvus、etcd、MinIO |

---

## 4. 总体架构

```text
User / Browser
    |
    | HTTP / SSE
    v
FastAPI API Layer
    |
    +-- Settings / Auth / Profile / Skills
    +-- Knowledge API
    +-- Research API
    |
    v
TaskManager
    |
    +-- QueryRouter
    |       |
    |       +-- direct_search -> SearchTool -> DirectAnswerService
    |       |
    |       +-- multi_agent
    |
    v
Workflow Engine Dispatcher
    |
    +-- LangGraph Workflow
    |
    +-- Harness Agent Runtime
    |
    v
Planner -> Researcher -> Writer -> Reviewer -> Formatter
    |
    v
ToolRouter
    |
    +-- SearchTool
    +-- BrowserTool
    +-- PythonTool / DisabledPythonTool
    +-- RAGRetrieverTool
    +-- WorkspaceReaderTool
    |
    v
Storage
    |
    +-- SQLite: tasks / reports / profiles / skills
    +-- Milvus: child chunk vector index
    +-- JSON index: BM25 child chunks / parent chunks
    +-- File system: reports / workspaces / knowledge docs
    +-- Redis: optional worker runtime
```

---

## 5. 任务执行流程

### 5.1 请求入口

前端调用：

```http
POST /api/research
```

核心参数：

```json
{
  "task": "分析 2026 年 AI Agent 行业趋势",
  "max_iterations": 3,
  "format": "markdown",
  "use_rag": true,
  "engine": "langgraph"
}
```

`engine` 支持：

| 值 | 含义 |
| --- | --- |
| `langgraph` | 使用 LangGraph 状态图执行多 Agent 工作流 |
| `harness` | 使用 Harness Agent Runtime 顺序调度同一套 Agent |

### 5.2 智能任务路由

`TaskManager` 启动任务后，先调用 `QueryRouter` 判断路线。

直接搜索路线适合：

- 简单事实问答
- 简短概念解释
- 明确单点问题

多 Agent 路线适合：

- 调研、调查、研究、趋势、发展、分析、报告
- 多维度比较
- 需要多轮检索和交叉验证的问题
- 需要 RAG 知识库参与的问题

路线结果：

```text
direct_search -> SearchTool -> LLM 总结 -> 报告输出
multi_agent   -> Workflow Engine -> 四 Agent 协作
```

该设计减少了简单问题进入完整多 Agent 工作流带来的模型调用成本和响应延迟。

---

## 6. 双引擎设计

### 6.1 LangGraph Workflow

LangGraph 使用 `ResearchState` 作为共享状态，在图节点之间传递任务计划、研究数据、报告草稿、审查评分和最终报告。

```text
planner -> researcher -> writer -> reviewer -> formatter
             ^                         |
             |                         |
             +------ conditional ------+
```

核心节点：

| 节点 | 职责 |
| --- | --- |
| `planner_node` | 根据用户任务生成研究视角和子任务计划 |
| `executor_node` | 并行执行若干 Researcher 子任务 |
| `writer_node` | 基于研究数据生成报告草稿 |
| `reviewer_node` | 多角色评审报告质量并给出评分 |
| `formatter_node` | 生成最终报告并保存到 KnowledgeMemory |

`router_decision` 根据 `current_step`、`report_draft`、`review_score`、`iteration_count` 决定下一跳。

### 6.2 Harness Agent Runtime

Harness 路线不使用 LangGraph 图，而是显式控制执行循环：

```text
plan()
research()
write_and_review()
format()
```

它复用同一套：

- `PlannerAgent`
- `ResearcherAgent`
- `WriterAgent`
- `ReviewerAgent`
- `ToolRouter`
- `ResearchState`

因此两种引擎在业务能力上保持一致，区别主要在编排方式：

| 维度 | LangGraph | Harness Agent Runtime |
| --- | --- | --- |
| 控制方式 | 状态图 + 条件边 | 显式顺序循环 |
| 可视化 | 图结构清晰 | 调度逻辑直观 |
| 扩展复杂流程 | 更适合复杂分支 | 更适合轻量运行时封装 |
| 当前定位 | 主工作流 | 可切换实验运行时 |

---

## 7. Agent 设计

### 7.1 PlannerAgent

Planner 负责将复杂任务拆成可执行子任务，并可生成 STORM 风格研究视角。

输出结构：

```json
{
  "perspectives": ["市场规模", "核心技术", "落地场景"],
  "plan": [
    {
      "id": "market-size",
      "description": "搜索 2026 年 AI Agent 市场规模和增长率",
      "tool": "search"
    }
  ]
}
```

工具选择规则：

- `search`：查找网页资料、新闻、报告、案例、市场数据
- `browse`：仅当任务中存在明确 URL 时使用
- `rag`：检索项目知识库
- `analyze`：对已有数据做结构化分析
- `read_workspace`：读取用户上传的任务参考文件

### 7.2 ResearcherAgent

Researcher 负责执行子任务并提取可写入报告的研究材料。

当前增强点：

- search 子任务会先检索，再自动浏览 Top URL
- browse 没有 URL 时自动改走 search，避免 `Not a valid HTTP URL`
- 工具失败不会伪造成事实，而是生成明确失败说明
- mock 搜索结果默认不会进入报告
- RAG 结果保留 `[来源: 文档名称]` 引用信息

### 7.3 WriterAgent

Writer 负责将多轮研究数据组织为结构化报告。

主要输入：

- 用户原始任务
- Planner 研究视角
- Researcher 研究摘要
- 来源列表
- Profile / Skills 注入的写作偏好

输出为 Markdown 报告草稿。

### 7.4 ReviewerAgent

Reviewer 负责对报告进行质量评审。

当前评审维度：

- 信息完整性
- 逻辑连贯性
- 引用可靠性
- 是否回答原始问题
- 是否存在明显无来源论断

达到通过阈值后进入 Formatter；未达标时回到 Writer 重写，最多执行 `max_iterations` 轮。

---

## 8. Tool Router 设计

`ToolRouter` 是 Agent 调用外部能力的统一入口。

```text
Agent -> ToolRouter.execute(name, params) -> BaseTool.execute()
```

默认工具：

| 工具 | 类 | 说明 |
| --- | --- | --- |
| search | `SearchTool` | 多搜索后端聚合 |
| browse | `BrowserTool` | Playwright/httpx 抓取网页和 PDF |
| analyze | `PythonTool` / `DisabledPythonTool` | Python 分析工具，默认禁用 |
| rag | `RAGRetrieverTool` | 调用 Hybrid RAG 检索器 |
| read_workspace | `WorkspaceReaderTool` | 读取任务上传文件 |
| memory | `MemoryTool` | 读写记忆信息 |

扩展新工具只需：

1. 继承 `BaseTool`
2. 实现 `name`、`description`、`parameters`
3. 实现 `execute`
4. 在 `ToolRouter._register_defaults()` 中注册

---

## 9. 搜索系统设计

`SearchTool` 采用可插拔搜索后端。

配置项：

```env
SEARCH_BACKENDS=tavily,duckduckgo,github,exa
TAVILY_API_KEY=
GITHUB_TOKEN=
EXA_API_KEY=
ENABLE_MOCK_SEARCH=false
```

执行流程：

```text
query
  |
  +-- TavilyBackend
  +-- DuckDuckGoBackend
  +-- GitHubBackend
  +-- ExaBackend
  |
  v
结果合并 -> URL/title 去重 -> 来源排序 -> ToolResult
```

默认禁止 mock 数据进入报告。搜索失败时返回明确错误，DirectAnswerService 或 ResearcherAgent 会将其标注为不可用资料，而不是生成假结论。

---

## 10. RAG 系统设计

### 10.1 设计目标

当前 RAG 的目标不是简单向量相似度搜索，而是同时满足：

- 语义相似召回
- 关键词精确召回
- 缩写、数字、实体名召回
- 长上下文补全
- 来源可追溯
- Milvus 不可用时优雅降级

### 10.2 导入流程

```text
文档内容
  |
  v
Parent Chunker
  |
  +-- parent chunk 1
  +-- parent chunk 2
  |
  v
Child Chunker
  |
  +-- child chunk 1.1
  +-- child chunk 1.2
  +-- child chunk 2.1
  |
  +-- ParentDocStore: 保存 parent 原文和 child_ids
  +-- BM25Store: 保存 child 文本并建立关键词索引
  +-- MilvusVectorStore: 保存 child embedding
```

分块配置：

```env
RAG_PARENT_CHUNK_SIZE=1800
RAG_PARENT_CHUNK_OVERLAP=200
RAG_CHILD_CHUNK_SIZE=512
RAG_CHILD_CHUNK_OVERLAP=64
```

父子节点设计：

| 节点 | 用途 |
| --- | --- |
| child chunk | 用于 BM25 和向量检索，粒度更细，召回更准 |
| parent chunk | 最终返回给 Writer，提供更完整上下文 |

### 10.3 检索流程

```text
用户 query
  |
  +-- Milvus 向量检索 topK
  |
  +-- BM25 关键词检索 topK
  |
  v
RRF 融合 child chunk 排名
  |
  v
按 parent_id 聚合
  |
  v
回查 parent chunk 原文
  |
  v
返回 parent 结果 + matched_children + score 信息
```

RRF 公式：

```text
score(d) = Σ 1 / (k + rank_i(d))
```

项目默认：

```env
RAG_VECTOR_TOP_K=20
RAG_BM25_TOP_K=20
RAG_RRF_K=60
```

RRF 的优势是不用直接比较向量分数和 BM25 分数，因为两者分数尺度不同。它只使用排名位置融合，更适合多检索器组合。

### 10.4 返回结构

知识库搜索接口返回示例：

```json
{
  "text": "parent chunk 原文",
  "metadata": {
    "source": "enterprise_rag_architecture",
    "doc_type": "markdown",
    "parent_id": "xxx",
    "retrieval_mode": "hybrid_rrf_parent_child",
    "child_count": 3
  },
  "score": 0.031,
  "rrf_score": 0.031,
  "vector_score": 0.78,
  "bm25_score": 28.63,
  "matched_children": ["命中的 child chunk"],
  "child_hits": []
}
```

前端展示为：

```text
排序: Top 1 · 融合得分: 0.031 · BM25: 28.63 · 向量命中: 是
```

### 10.5 向量库降级策略

默认配置：

```env
RAG_VECTOR_BACKEND=milvus
MILVUS_URI=http://localhost:19530
```

当 Milvus 无法连接时：

1. 系统尝试降级到 ChromaDB
2. 如果 ChromaDB 集合维度不匹配或查询失败，向量召回置空
3. BM25 继续工作
4. RRF 仍可基于 BM25 排名返回结果

这可以避免知识库接口因为向量库问题直接 500。

---

## 11. 数据存储设计

### 11.1 SQLite

SQLite 负责业务数据持久化：

| 表 | 说明 |
| --- | --- |
| `tasks` | 任务状态、报告、审查分数、错误、sources、research_data、total_tokens |
| `reports` | 历史报告内容 |
| `task_events` | Agent 事件日志 |
| `skills` | 系统技能和用户自定义技能 |
| `user_profiles` | 用户写作偏好、领域偏好、模型偏好 |
| `user_skill_prefs` | 用户对全局技能的启用/禁用 |
| `evolution_drafts` | 任务结束后生成的技能优化草稿 |

### 11.2 RAG 本地索引

| 文件 | 说明 |
| --- | --- |
| `data/rag_index/parent_chunks.json` | parent chunk 原文和元数据 |
| `data/rag_index/bm25_chunks.json` | child chunk 文本和 BM25 索引数据 |
| `data/chroma_db/` | ChromaDB 兜底向量数据 |

### 11.3 Milvus

Milvus 存储 child chunk embedding。

核心字段：

| 字段 | 说明 |
| --- | --- |
| `chunk_id` | child chunk 唯一 ID |
| `text` | child chunk 文本 |
| `embedding` | 向量 |
| `metadata` | source、doc_type、parent_id、child_index 等 |

### 11.4 Workspace

每个研究任务拥有独立工作目录：

```text
data/workspaces/{task_id}/
```

用户上传的 PDF、Markdown、TXT、CSV、JSON、DOCX 会保存到该目录。`WorkspaceReaderTool` 可在 Agent 执行过程中读取这些参考文件。

---

## 12. API 设计

### 12.1 研究任务

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | `/api/research` | 创建并启动研究任务 |
| POST | `/api/research/prepare` | 创建任务但暂不启动，用于先上传文件 |
| POST | `/api/research/{task_id}/upload` | 上传任务参考文件 |
| POST | `/api/research/{task_id}/start` | 启动已准备任务 |
| GET | `/api/research/{task_id}/stream` | SSE 实时事件流 |
| GET | `/api/research/{task_id}` | 查询任务状态 |
| GET | `/api/history` | 查询历史任务 |
| POST | `/api/reports/batch-delete` | 批量删除历史报告 |

### 12.2 报告

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/api/reports/{task_id}?format=markdown` | 下载 Markdown |
| GET | `/api/reports/{task_id}?format=pdf` | 下载 PDF |

### 12.3 知识库

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| POST | `/api/knowledge/ingest` | 导入文档到 RAG |
| GET | `/api/knowledge/search?q=xxx&k=5` | Hybrid RAG 检索 |
| GET | `/api/knowledge/list` | 列出知识库文档 |
| DELETE | `/api/knowledge/docs` | 按 source 删除文档 |

### 12.4 设置、偏好与技能

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/api/settings` | 获取 LLM 配置 |
| POST | `/api/settings` | 保存并测试 LLM 配置 |
| GET | `/api/profile` | 获取当前用户偏好 |
| PUT | `/api/profile` | 更新当前用户偏好 |
| GET | `/api/skills` | 获取技能列表 |
| POST | `/api/skills` | 创建自定义技能 |
| PUT | `/api/skills/{skill_id}` | 更新技能 |
| DELETE | `/api/skills/{skill_id}` | 删除技能 |
| POST | `/api/skills/match` | 测试任务会匹配哪些技能 |

---

## 13. SSE 事件设计

前端为每个运行中任务建立独立 SSE 连接：

```text
GET /api/research/{task_id}/stream
```

事件类型：

| 事件 | 用途 |
| --- | --- |
| `connected` | SSE 连接成功 |
| `agent_status` | Agent 或系统状态变化 |
| `agent_result` | Agent 节点完成 |
| `skills_matched` | 匹配到可注入技能 |
| `tool_call` | 工具开始调用 |
| `tool_result` | 工具返回结果 |
| `report_chunk` | 报告分块流式输出 |
| `node_error` | 工作流节点错误 |
| `completed` | 任务完成 |
| `error` | 任务失败 |

上游更新后，事件回调从全局变量改为 `contextvars.ContextVar`，使并发任务之间不会串流。

---

## 14. 前端设计

前端使用 React + Tailwind CSS + Vite。

核心组件：

| 组件 | 说明 |
| --- | --- |
| `App.jsx` | 应用主入口，多任务状态管理、SSE 连接、历史记录 |
| `InputPanel.jsx` | 研究输入、深度选择、引擎选择、RAG 开关、知识库弹窗 |
| `AgentTrace.jsx` | 当前 Agent 执行状态和工具调用轨迹 |
| `ReportViewer.jsx` | Markdown 报告渲染、引用侧栏、MD/PDF 下载 |
| `SettingsModal.jsx` | LLM、Embedding、Reranker 配置 |
| `SkillsModal.jsx` | 技能管理和演化草稿 |
| `ProfileModal.jsx` | 用户偏好设置 |
| `AuthModal.jsx` | 登录注册 |

前端启动时通过 Vite 代理访问后端：

```js
proxy: {
  '/api': 'http://127.0.0.1:8001'
}
```

---

## 15. 配置说明

关键环境变量：

```env
# Server
HOST=0.0.0.0
PORT=8000
LOG_LEVEL=info

# LLM
OPENAI_API_KEY=
OPENAI_BASE_URL=
DASHSCOPE_API_KEY=
ANTHROPIC_API_KEY=
LLM_MODEL_PLANNER=gpt-4o
LLM_MODEL_RESEARCHER=gpt-4o
LLM_MODEL_WRITER=gpt-4o
LLM_MODEL_REVIEWER=gpt-4o

# Workflow
WORKFLOW_ENGINE=langgraph
QUERY_ROUTER_ENABLED=true
DIRECT_SEARCH_MAX_RESULTS=5

# Search
SEARCH_BACKENDS=tavily,duckduckgo,github
TAVILY_API_KEY=
GITHUB_TOKEN=
EXA_API_KEY=
ENABLE_MOCK_SEARCH=false

# RAG
RAG_VECTOR_BACKEND=milvus
RAG_INDEX_PATH=./data/rag_index
RAG_PARENT_CHUNK_SIZE=1800
RAG_CHILD_CHUNK_SIZE=512
RAG_VECTOR_TOP_K=20
RAG_BM25_TOP_K=20
RAG_RRF_K=60

# Milvus
MILVUS_URI=http://localhost:19530
MILVUS_COLLECTION=research_knowledge
MILVUS_METRIC_TYPE=COSINE

# Database
DATABASE_URL=sqlite+aiosqlite:///./data/research.db
REDIS_URL=redis://localhost:16379/0

# Workspace
WORKSPACE_ROOT=./data/workspaces
UPLOAD_MAX_FILES=10
UPLOAD_MAX_BYTES=20971520
UPLOAD_ALLOWED_EXTS=.pdf,.md,.txt,.csv,.json,.docx
```

---

## 16. 本地启动

### 16.1 启动数据库

```powershell
cd D:\codexcode\DeepResearch-Agent
docker compose up -d
```

Docker Compose 会启动：

- Redis
- etcd
- MinIO
- Milvus Standalone

### 16.2 启动后端

```powershell
cd D:\codexcode\DeepResearch-Agent

$env:REDIS_URL="redis://localhost:16379/0"
$env:MILVUS_URI="http://localhost:19530"
$env:RAG_VECTOR_BACKEND="milvus"

.\.venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8001
```

健康检查：

```powershell
Invoke-RestMethod http://127.0.0.1:8001/health
```

### 16.3 启动前端

```powershell
cd D:\codexcode\DeepResearch-Agent\app\web
npm run dev -- --host 127.0.0.1 --port 15174 --strictPort
```

访问：

```text
http://127.0.0.1:15174/
```

---

## 17. 关键设计取舍

### 17.1 为什么保留 LangGraph，同时加入 Harness？

LangGraph 适合展示有状态图、条件边和循环审查流程，是复杂 Agent 编排的主路径。Harness Agent Runtime 适合更直接地控制执行顺序，为后续接入更标准化的 Agent Runtime 留出适配面。两者复用同一套 Agent 和 Tool，避免业务逻辑分叉。

### 17.2 为什么使用 BM25 + 向量融合？

向量检索适合语义相似问题，但对缩写、精确术语、数字、版本号并不总是稳定。BM25 对关键词、实体名和短语更敏感。RRF 通过排名融合两路召回，避免直接比较不同分数体系，使结果更稳健。

### 17.3 为什么使用父子节点？

如果直接返回小 chunk，Writer 可能缺少上下文；如果直接检索大 chunk，召回精度会下降。父子节点将“精准召回”和“完整上下文”拆开：child chunk 用于检索，parent chunk 用于回答。

### 17.4 为什么 Milvus 不可用时不直接失败？

本项目既要支持生产级向量库，也要保证本地学习和演示可用。因此 Milvus 是默认主路径，但检索层允许降级到 ChromaDB 或 BM25-only，避免数据库暂时不可用导致整个知识库功能不可用。

---

## 18. 简历亮点总结

本项目可总结为：

1. 基于 LangGraph 构建 Planner、Researcher、Writer、Reviewer 四 Agent 协作流程，并扩展 Harness Agent Runtime 双引擎切换。
2. 设计智能任务路由，将简单问题直接联网搜索回答，复杂调研任务进入多 Agent 深度研究，降低不必要的模型调用成本。
3. 实现插件式 Tool Router，统一接入搜索、网页浏览、Python 执行、RAG 检索和 Workspace 文件读取。
4. 重构 RAG 为 Milvus 向量检索 + BM25 关键词检索 + RRF 融合排序，并引入父子分块提升召回准确性和上下文完整性。
5. 基于 FastAPI + SSE + React 实现多任务并行、Agent 执行轨迹和报告内容实时流式展示。
6. 使用 SQLite 持久化任务、报告、技能和用户偏好，结合 Docker Compose 管理 Redis、Milvus 等本地基础设施。
